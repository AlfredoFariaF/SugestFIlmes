<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8" %><%@ taglib prefix="c" uri="jakarta.tags.core" %>
<!DOCTYPE html><html lang="pt-BR"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Recomendações - CineVault</title><link rel="stylesheet" href="${pageContext.request.contextPath}/css/estilo.css"></head><body>
<header class="topbar"><div class="container topbar-content"><a class="logo" href="${pageContext.request.contextPath}/home"> CineVault</a><nav class="nav"><a href="${pageContext.request.contextPath}/home">Início</a><a href="${pageContext.request.contextPath}/filmes">Filmes</a><a href="${pageContext.request.contextPath}/categorias">Categorias</a><a class="active" href="${pageContext.request.contextPath}/recomendacoes">Recomendações</a><a href="${pageContext.request.contextPath}/avaliacoes?minhas=true">Minhas avaliações</a></nav><a href="${pageContext.request.contextPath}/logout">Sair</a></div></header>
<main class="container page-space"><div class="page-header"><div><h1>Recomendações</h1><p>Os filmes são ordenados pela quantidade de categorias escolhidas e pela média de avaliações.</p></div></div><form method="get" action="${pageContext.request.contextPath}/recomendacoes" class="card recommendation-filter"><div class="category-grid"><c:forEach var="categoria" items="${categorias}"><label class="category-option"><input type="checkbox" name="categoria" value="${categoria.id}" <c:if test="${selecionadas.contains(categoria.id)}">checked</c:if>><span>${categoria.nome}</span></label></c:forEach></div><div class="center-actions"><button class="btn" type="submit">Atualizar recomendações</button></div></form><c:choose><c:when test="${empty selecionadas}"><div class="empty">Escolha pelo menos uma categoria para receber sugestões.</div></c:when><c:when test="${empty filmes}"><div class="empty">Nenhum filme possui as categorias selecionadas.</div></c:when><c:otherwise><div class="movie-grid"><c:forEach var="filme" items="${filmes}"><article class="movie-card"><div class="movie-poster"><span></span><span class="match-badge">${filme.correspondencias} categoria(s)</span></div><div class="movie-body"><h3>${filme.titulo}</h3><p class="movie-meta">${filme.genero} · ${filme.ano}</p><p class="match-text">Combina com <strong>${filme.correspondencias}</strong> das categorias escolhidas.</p><div class="rating">Nota: <strong><c:choose><c:when test="${filme.totalAvaliacoes > 0}">${filme.mediaAvaliacao}</c:when><c:otherwise>Sem avaliações</c:otherwise></c:choose></strong></div><a class="btn btn-secondary btn-small" href="${pageContext.request.contextPath}/filmes?acao=detalhes&id=${filme.id}">Ver detalhes</a></div></article></c:forEach></div></c:otherwise></c:choose></main></body></html>
